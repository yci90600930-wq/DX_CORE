# 프로젝트 핵심 작업 기록

모든 일시의 기준 시간대는 Asia/Seoul(KST)입니다. 핵심 기능 추가·수정·제거 작업이 끝날 때마다 최신 기록을 아래에 추가합니다.

## 2026-08-04 15:45:33 KST

### 현재 핵심 상태

- 순수 HTML, CSS, JavaScript 구조로 정부지원사업 검색 화면을 구성했습니다.
- 오늘 신규 공고, 관심 키워드 임시 저장, 제목·본문 OR 검색, 중복 공고 통합과 내부 상세 공고문 확인 흐름이 있습니다.
- 접수기간이 지나지 않은 통합 공고를 마감일 순서로 표시하고 30분 단위 갱신 주기를 선택할 수 있습니다.
- design.md의 색상·글꼴·화면 순서를 적용하고 800px, 640px, 380px 이하 반응형 규칙을 적용했습니다.
- 핵심 기능 추가·수정·제거 후 이 파일에 일시와 변경 내용을 기록하는 규칙을 AGENTS.md에 추가했습니다.

### 확인 필요

- 공고 데이터는 현재 assets/js/app.js의 데모 데이터이며 실제 정부기관 공고 재수집은 아직 연동되지 않았습니다.
- 자동화 테스트, 배포 환경, 운영 URL과 데이터 저장 정책은 [확인 필요]입니다.

## 2026-08-04 16:05:38 KST

### 핵심 수정

- 루트에는 실행 진입점 index.html과 작업 지침 AGENTS.md만 유지했습니다.
- PRD.md, design.md, memory.md를 docs 폴더로 이동했습니다.
- styles.css를 assets/css로, app.js를 assets/js로 이동했습니다.
- index.html과 AGENTS.md의 참조 경로를 새 폴더 구조에 맞게 수정했습니다.

## 2026-08-04 16:30:27 KST

### 핵심 기능 추가

- 이메일·비밀번호 회원가입, 로그인, 로그아웃과 비밀번호 재설정 화면을 추가했습니다.
- Supabase Auth와 사용자별 관심 키워드 저장을 연결하는 assets/js/auth.js를 추가했습니다.
- 로그인 전 임시 키워드는 계정에 합치지 않고, 로그인 성공 시 계정 키워드로 교체하도록 구현했습니다.
- user_keywords 테이블과 사용자 본인 데이터만 조회·추가·삭제할 수 있는 RLS 정책을 supabase/schema.sql에 추가했습니다.
- 로그인 기능의 제품 범위와 화면 기준을 docs/PRD.md와 docs/design.md에 반영했습니다.
- Supabase 연결 방법과 공개 키 보안 기준을 docs/supabase-setup.md에 기록했습니다.

### 확인 필요

- 실제 로그인 활성화에는 Supabase Project URL과 Publishable key 입력이 필요합니다.
- 이메일 확인과 비밀번호 재설정에 사용할 실제 배포 주소 및 운영 SMTP는 [확인 필요]입니다.

## 2026-08-05 17:50:11 KST

### 핵심 수정

- 관심 키워드 저장·검색 영역을 오늘의 신규 공고 영역 위로 이동했습니다.
- 오늘 공고 요약의 중기부·소진공·기업마당·스마트공장 기관명을 클릭 가능한 기관 선택 버튼으로 변경했습니다.
- 선택 기관의 공고만 오늘 신규 공고와 마감되지 않은 전체 공고에 표시하고, 같은 기관을 다시 누르면 전체 기관 공고로 돌아가도록 구현했습니다.
- 선택 기관 상태, 공고 수와 적용 조건 안내가 함께 갱신되며 모바일에서는 기관 버튼이 두 열로 유지되도록 했습니다.
- 화면 순서와 기관 선택 기능을 docs/PRD.md와 docs/design.md에 반영했습니다.

## 2026-08-11 10:37:38 KST

### 핵심 화면 효과 추가

- React Bits의 공식 LightRays JS-CSS registry와 원본 WebGL 셰이더를 기준으로 메인 소개 영역에 빛줄기 배경을 추가했습니다.
- 프로젝트의 순수 HTML·CSS·JavaScript 및 무설치 규칙을 지키기 위해 React, shadcn과 `ogl` 패키지는 설치하지 않고 순수 WebGL로 변환했습니다.
- 위 중앙 시작, `#00ffff`, 속도 1.5, 빛 확산 0.8, 길이 1.2, 마우스 영향 0.1, 노이즈 0.1과 왜곡 0.05 설정을 적용했습니다.
- 화면 밖에서는 애니메이션을 멈추고, 동작 줄이기 설정에서는 정지 화면으로 표시되도록 했습니다.
- 600px 최소 높이와 반응형 레이아웃을 유지해 모바일에서도 본문과 버튼이 화면 밖으로 밀려나지 않도록 했습니다.

## 2026-08-11 10:41:43 KST

### 기관 선택 버튼 디자인 수정

- Animata Shift Tabs 공식 registry를 기준으로 조회 대상 기관 네 개 버튼만 수정했습니다.
- 비선택 버튼에 마우스를 올리면 오른쪽 위를 기준으로 6도 기울고, 선택 버튼은 회전 없이 청록색 테두리로 강조되도록 적용했습니다.
- 키보드 방향키와 Home·End 키로 기관 버튼 사이를 이동할 수 있게 했습니다.
- 기존 기관별 공고 필터 동작, 모바일 두 열 배치와 동작 줄이기 설정을 유지했습니다.
- 프로젝트 규칙에 따라 React, shadcn과 패키지는 설치하지 않았습니다.

## 2026-08-11 10:45:39 KST

### 기관별 진행 중 공고 숫자 추가

- Animata Reminder 공식 registry를 기준으로 네 기관 선택 버튼의 기관명 아래에 현재 접수 진행 중인 공고 숫자를 추가했습니다.
- 접수 시작일이 지나지 않은 접수 예정 공고는 숫자에서 제외하고, 접수 중인 통합 공고를 실제 출처별로 집계하도록 구현했습니다.
- 여러 기관에 등록된 통합 공고는 해당하는 각 기관의 숫자에 각각 포함됩니다.
- 기존 Shift Tabs 효과, 기관별 필터 동작과 모바일 두 열 배치를 유지했습니다.
- React, shadcn과 별도 패키지는 설치하지 않았습니다.

## 2026-08-11 11:08:58 KST

### 메인 제목 TextLoop 적용

- React Bits TextLoop JS-CSS 공식 registry를 기준으로 메인 제목만 물결형 SVG 반복 애니메이션으로 변경했습니다.
- 제품 문맥을 유지하기 위해 예시 문구 대신 기존 제목 `우리 회사에 맞는 지원사업 ✦ 찾는 시간부터 줄이세요.`를 사용했습니다.
- 속도 90, 정방향, 곡률 90, 글자 크기 46, 굵기 800, 자간 2, 흰색 글자, `#5227FF` 리본과 리본 너비 86을 적용했습니다.
- 마우스를 올리면 일시 정지하고, 화면 밖과 동작 줄이기 설정에서는 애니메이션이 멈추도록 했습니다.
- 프로젝트 규칙에 따라 React, shadcn과 `gsap` 패키지는 설치하지 않고 순수 SVG·JavaScript로 변환했습니다.

## 2026-08-11 11:12:41 KST

### TextLoop 적용 위치 변경

- 메인 제목의 TextLoop 효과를 제거하고 `우리 회사에 맞는 지원사업, 찾는 시간부터 줄이세요.` 두 줄 제목으로 복원했습니다.
- React Bits TextLoop 효과를 상단 `DX CORE` 브랜드명으로 옮기고 반복 문구를 `DX ✦ CORE`로 변경했습니다.
- 속도 90, 정방향, 곡률 90, 글자 크기 46, 굵기 800, 자간 2, 흰색 글자, `#5227FF` 리본과 리본 너비 86 설정은 유지했습니다.
- 헤더와 모바일 화면에 맞게 SVG 화면 크기만 축소하고 접근성용 브랜드명 `DX CORE`를 유지했습니다.

## 2026-08-11 11:30:13 KST

### AI 개발팀 구성

- 프로젝트 전용 Codex 커스텀 에이전트 설정을 `.codex/agents/`에 추가했습니다.
- 짱구는 HTML, 철수는 CSS, 훈이는 JavaScript, 맹구는 읽기 전용 QA를 담당하도록 역할과 작업 경계를 지정했습니다.
- 각 에이전트가 이름이나 역할을 질문받았을 때 지정된 문장으로 답하도록 설정했습니다.
- `.codex/config.toml`에서 멀티에이전트 기능을 활성화하고 동시에 열 수 있는 서브에이전트 스레드 수를 4개로 설정했습니다.
- `AGENTS.md`에 메인 에이전트의 위임 기준, 파일 충돌 방지와 변경 후 QA 규칙을 추가했습니다.
- 초보자가 바로 사용할 수 있도록 `docs/ai-team.md`에 팀 구성과 호출 예시를 기록했습니다.

## 2026-08-11 11:50:50 KST

### 로그인 화면 교체

- 메인 화면의 기존 로그인 대화상자를 폐기하고 별도 `login.html` 페이지로 교체했습니다.
- 새 로그인 페이지 한 곳에서 이메일·비밀번호 로그인, 회원가입, 비밀번호 재설정 이메일 요청과 새 비밀번호 저장을 처리하도록 구성했습니다.
- 인증 화면 동작을 `assets/js/login.js`로 분리하고, `assets/js/app.js`에는 로그인 상태 표시, 로그아웃과 계정 관심 키워드 연동만 유지했습니다.
- 로그인 성공 후 메인 공고 화면으로 돌아가며, 로그인 전 임시 키워드는 계정에 합치지 않는 기존 기준을 유지했습니다.
- 640px 이하에서 인증 버튼을 한 열로 배치하고 입력창과 버튼이 화면 밖으로 나가지 않도록 했습니다.
- Supabase 이메일 확인과 비밀번호 복구의 Redirect URL 기준을 배포된 `login.html` 주소로 변경했습니다.
- 비밀번호 복구 모드에서는 숨겨진 이메일 입력의 필수 검사를 해제하고, 브라우저 인증 설정은 `sb_publishable_` 공개 키 형식만 허용하도록 검증했습니다.

### 확인 필요

- 실제 로그인 사용에는 `assets/js/supabase-config.js`에 Project URL과 Publishable key 입력이 필요합니다.
- 이메일 확인과 비밀번호 재설정에는 실제 HTTP(S) 배포 주소 및 운영 SMTP가 필요합니다.

## 2026-08-11 12:04:16 KST

### 회원가입 설정 오류 안내 수정

- Supabase 연결 정보가 없을 때 회원가입 버튼이 아무 설명 없이 비활성화되던 문제를 수정했습니다.
- 연결 전에도 회원가입 버튼을 누를 수 있게 하고, 제출하면 필요한 Project URL과 Publishable key를 화면에 명확히 안내하도록 변경했습니다.
- 현재 프로젝트와 `백업_20260811` 모두 공개 연결 정보가 비어 있어 실제 계정 생성은 아직 활성화되지 않은 상태입니다.

### 확인 필요

- 실제 회원가입 활성화를 위해 Supabase Project URL과 `sb_publishable_`로 시작하는 Publishable key가 필요합니다.
- 이메일 확인에는 HTTP(S)로 배포된 `login.html` 주소를 Supabase Redirect URLs에 등록해야 합니다.

## 2026-08-11 12:37:50 KST

### GitHub 업로드 준비

- 현재 프로젝트 폴더를 `main` 브랜치의 로컬 Git 저장소로 초기화했습니다.
- GitHub 원격 저장소 `yci90600930-wq/DX_CORE`를 `origin`으로 연결했습니다.
- 운영체제·편집기 임시 파일, 환경 변수와 로그 파일이 업로드되지 않도록 `.gitignore`를 추가했습니다.
- 원격 저장소가 비어 있음을 확인했으며, 이번 작업에서는 파일을 스테이징·커밋·푸시하지 않았습니다.

## 2026-08-11 13:38:14 KST

### GitHub 첫 업로드

- 프로젝트 전체 파일을 GitHub 저장소 `yci90600930-wq/DX_CORE`의 `main` 브랜치에 올리는 범위로 확정했습니다.
- GitHub CLI 로그인과 Git 인증 연결을 완료하고, 커밋 작성자 이메일은 GitHub 비공개 이메일 형식으로 설정했습니다.
- 업로드 전 Secret key, service_role key와 GitHub 토큰 패턴이 포함되지 않았는지 검사했습니다.
- `assets/js`의 모든 JavaScript 파일에 대해 문법 검사를 통과했습니다.

## 2026-08-11 14:28:39 KST

### 배포 전 보안·준비 점검

- Sites 배포 전 비밀키, 토큰, 개인정보와 공개 불필요 파일을 점검하고 `docs/deployment-readiness.md`에 결과를 기록했습니다.
- Supabase 브라우저 라이브러리를 `2.112.2`로 고정하고 SHA-384 무결성 검사를 추가했습니다.
- `index.html`과 `login.html`에 콘텐츠 보안 정책과 Referrer Policy를 추가했습니다.
- 운영 배포물은 두 HTML과 `assets`의 실행 파일로만 제한하고 `.codex`, `docs`, `supabase`, `AGENTS.md` 등 개발 파일을 제외하도록 범위를 확정했습니다.
- 실제 공고 수집, Supabase 운영 연결과 Sites용 런타임 산출물이 없어 현재 운영 배포 판정은 `NO-GO`입니다.
- 사용자 요청에 따라 Sites 프로젝트 생성과 실제 배포는 진행하지 않았습니다.

## 2026-08-11 15:03:39 KST

### 실제 정부 공고 수집과 Sites 배포 전 구조 추가

- 화면 확인용 데모 공고 배열을 제거하고 `/api/notices`의 실제 공식 공고만 표시하도록 변경했습니다.
- 순수 JavaScript Worker에서 중기부 공식 사업공고 목록, 소진공 공식 사업공고 목록, 스마트공장 공식 JSON을 각각 직접 수집하고 기업마당 공식 API를 선택적으로 통합하도록 구현했습니다.
- 기관별 수집 실패를 분리하고 5분 서버 캐시, 브라우저 새로고침 시 강제 재수집, 선택 주기에 따른 자동 갱신을 연결했습니다.
- 접수 종료일이 지나지 않은 공고만 통합 목록에 표시하고 개별 원문과 실제 첨부파일 링크를 상세 화면에 연결했습니다.
- Sites 프로젝트를 생성해 `.openai/hosting.json`에 프로젝트 ID를 저장하고 공개 허용 파일만 `dist/`에 분리했습니다. 실제 버전 저장과 공개 배포는 진행하지 않았습니다.
- 실제 공식 응답 검사에서 기업마당 인증키 없이 중기부 12건, 소진공 28건, 스마트공장 61건의 수집과 정규화를 확인했습니다.

### 확인 필요

- 기업마당 전체 공고를 추가하려면 `BIZINFO_API_KEY`를 Sites Secret 환경 변수로 등록해야 합니다.
- 회원가입·로그인에는 `SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`, RLS 적용과 운영 Redirect URL이 필요합니다.
- 첫 Sites 배포 후 정적 `ASSETS` 바인딩과 운영 URL에서 실제 화면·모바일·인증 흐름을 최종 확인해야 합니다.

## 2026-08-11 15:20:26 KST

### Sites 운영 배포

- 검증된 `dist` 배포물을 Sites 버전 1로 저장하고 비공개 우선 배포 절차를 완료했습니다.
- 운영 주소는 `https://dx-core-support-notices.yci90600930.chatgpt.site`입니다.
- 중기부·소진공·스마트공장 실제 공고 조회가 가능한 제한 운영판이며, 기업마당 API와 Supabase 인증은 환경 값이 없어 아직 비활성 상태입니다.
- 배포 전 원본과 `dist` 해시, JavaScript 문법, 공개 파일 범위와 비밀키 미포함 여부를 다시 확인했습니다.

### 확인 필요

- `BIZINFO_API_KEY`, `SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`, RLS와 Redirect URL을 등록한 뒤 전체 기능 배포가 필요합니다.

## 2026-08-11 15:36:12 KST

### 기업마당 API 운영 연결과 Sites 런타임 수정

- 기업마당 인증키를 코드·문서·Git이 아닌 Sites의 비밀 환경 변수 `BIZINFO_API_KEY`로 등록했습니다.
- Sites 런타임에서 허용되지 않는 기본 Cache API 호출을 제거해 공고 갱신 요청의 서버 오류를 수정했습니다.
- 수정한 원본과 `dist` 서버 파일을 같은 상태로 맞추고 Sites 버전 2로 재배포했습니다.
- 운영 API에서 기업마당 1,371건을 포함한 통합 공고 1,430건 반환을 확인했습니다.
- 같은 확인 시점에 스마트공장 59건은 정상 수집됐고, 소진공은 상류 오류, 중기부는 접수기간 필터 결과 0건으로 보고됐습니다.

### 확인 필요

- 소진공 상류 연결의 일시 오류 여부와 중기부 접수기간 파싱 결과를 별도로 재확인해야 합니다.
- Supabase 회원가입·로그인은 운영 공개 설정과 RLS·Redirect URL 등록이 필요합니다.

## 2026-08-11 15:47:14 KST

### Sites 첫 화면 404 원인 확인과 수정 준비

- 운영 공고 API는 정상 응답하지만 `/`, `/index.html`, `/login.html`과 CSS 파일이 404를 반환하는 문제를 확인했습니다.
- 배포 정적 파일이 Sites가 읽는 `dist/client/` 경로에 없었던 것이 원인이었습니다.
- 기존 화면 파일과 자산을 `dist/client/`에 동일하게 스테이징하고 원본과 해시가 일치하는지 확인했습니다.
- 수정 배포 버전 3을 저장했으며, 사이트가 공개 상태라 사용자 배포 승인을 기다리고 있습니다.

## 2026-08-11 15:52:25 KST

### Sites 첫 화면 404 수정 공개 배포

- 사용자 공개 배포 승인 후 정적 파일 경로 수정 버전 3을 운영 주소에 배포했습니다.
- 운영 주소의 `/`, `/index.html`, `/login.html`, CSS와 `/api/notices`가 모두 HTTP 200으로 응답하는 것을 확인했습니다.
- 첫 화면과 로그인 화면을 같은 운영 주소에서 다시 열 수 있도록 확인했습니다.

## 2026-08-12 10:36:30 KST

### 공공데이터포털 단일 API 수집으로 교체

- 기존 중기부·소진공·기업마당·스마트공장 개별 수집을 제거하고 공공데이터포털 `중소기업 지원사업 공고 조회 서비스` 단일 수집으로 교체했습니다.
- 인증키는 코드·문서·Git에 저장하지 않고 Sites 비밀 환경 변수 `DATA_GO_KR_API_KEY`로만 등록했습니다.
- 접수 종료일이 지나지 않은 공고만 표시하고, 동일한 `pblancId`는 수정일이 최신인 한 건만 유지하도록 변경했습니다.
- 전체·스마트공장·AI·AX·DX·R&D·정책자금·수출·기타 분류 버튼과 진행 공고 수를 연결했습니다.
- 실제 API 전체 페이지를 호출해 진행 공고 1,404건, 고유 공고 ID 1,404개와 7개 분류 집계를 확인했습니다.
- 원본과 `dist`, `dist/client` 배포 파일의 해시 일치, 전체 JavaScript 문법과 API 키 미포함 여부를 확인했습니다.

### 확인 필요

- 새 버전을 Sites에 공개 배포한 뒤 운영 URL에서 새로고침, 분류 필터, 상세 원문과 휴대폰 폭 화면을 최종 확인해야 합니다.
- Supabase 회원가입·로그인은 운영 공개 설정과 RLS·Redirect URL 등록이 필요합니다.

## 2026-08-12 11:08:27 KST

### 로컬 화면 공고 0건 원인 수정

- 새 화면을 `file://`로 열면 상대 주소 `/api/notices`를 호출할 서버가 없어 공고가 0건으로 보이던 원인을 확인했습니다.
- 로컬 화면에서는 Sites 운영 주소의 `/api/notices`를 사용하고, 배포 화면에서는 기존처럼 같은 주소의 API를 사용하도록 분리했습니다.
- 공개 공고 JSON 응답에 CORS 허용 헤더를 추가하고 CSP의 연결 허용 주소에 Sites 운영 주소를 반영했습니다.
- 현재 운영 API가 아직 이전 다중 수집 버전임을 확인했으며, 새 공공데이터포털 API 버전을 공개 배포해야 로컬 화면에도 새 형식의 공고가 표시됩니다.

## 2026-08-12 13:34:00 KST

### 공공데이터포털 버전 5 공개 배포와 갱신 안정화

- 사용자 승인 후 공공데이터포털 단일 API 화면과 서버를 Sites 운영 주소에 공개 배포했습니다.
- 운영 첫 화면은 HTTP 200, 일반 공고 API는 `data-go-kr` 제공자 기준 접수 진행 공고 1,404건을 반환하는 것을 확인했습니다.
- 강제 새로고침 중 상류 API 한 페이지가 일시 지연되어 전체 요청이 실패한 사례를 확인했습니다.
- 네트워크·시간초과·비정상 응답이 발생하면 해당 페이지를 한 번 더 호출하고, 두 번 모두 실패할 때만 일반 오류를 반환하도록 보강했습니다.

## 2026-08-12 14:06:29 KST

### 공고 제목 목록과 페이지 이동 추가

- 오늘의 신규 공고와 마감되지 않은 전체 공고를 상세 카드 대신 제목만 표시하는 목록으로 변경했습니다.
- 두 목록이 서로 독립적으로 한 페이지에 10개, 20개, 50개 또는 100개를 표시하도록 선택할 수 있게 했습니다.
- 각 목록에 이전·다음 버튼과 현재 페이지·전체 페이지 안내를 추가했습니다.
- 검색어, 관심 키워드, 분류 또는 새 데이터가 바뀌면 페이지를 첫 페이지로 초기화하고, 마지막 페이지 범위를 벗어나지 않게 보정합니다.
- 640px와 380px 휴대폰 폭에서 제목, 선택란과 페이지 이동 버튼이 화면 밖으로 나가지 않도록 반응형 배치를 적용했습니다.

## 2026-08-12 14:36:39 KST

### 기존 분류를 유지한 지역 표시 추가

- 스마트공장·AI·AX·DX·R&D·정책자금·수출·기타 기존 분류와 필터 동작은 변경하지 않았습니다.
- 공공데이터의 해시태그, 공고명, 사업개요와 기관명에서 전국 및 17개 시도 지역을 판별하도록 추가했습니다.
- 지역을 확인할 수 없는 공고는 `전국`으로 표시하고 여러 지역이 명시된 공고는 지역 배지를 함께 표시합니다.
- 오늘 신규 공고와 마감되지 않은 전체 공고의 제목 목록에 지역 배지를 표시하고 내부 상세 화면에 지원 지역을 추가했습니다.
- 여러 지역 배지가 휴대폰 폭에서 자동으로 줄바꿈되도록 반응형 스타일을 적용했습니다.
- 첫 공개 배포 검증에서 공공데이터 해시태그가 검색용으로 17개 지역 전체를 포함하는 공고가 있어 지역이 과도하게 표시되는 문제를 발견했습니다.
- 제목·사업개요·기관의 명확한 지역 표현은 유지하고, 지역 해시태그는 정확한 지역이 1~3개일 때만 반영하도록 수정했습니다. 4개 이상이면 검색용 분류 태그로 보고 지역 판별에서 제외합니다.

## 2026-08-12 15:21:08 KST

### 첨부 예시 형태의 지역별 공고 필터 추가

- 사용자의 첨부 예시 의도를 반영해 제목 옆 지역 배지뿐 아니라 별도의 지역 선택 버튼 줄을 추가했습니다.
- 스마트공장·AI·AX·DX·R&D·정책자금·수출·기타 기존 분류 버튼과 집계 방식은 그대로 유지했습니다.
- 기존 분류 아래에 `지역 전체`, `전국`과 17개 시도별 진행 공고 수를 여러 줄의 버튼으로 표시합니다.
- 지역 버튼을 누르면 기존 분류 조건과 지역 조건을 함께 적용해 오늘 신규 공고와 마감되지 않은 전체 공고를 거릅니다.
- 지역 선택 시 두 목록의 페이지를 첫 페이지로 초기화하고, 키보드 방향키·Home·End 이동과 선택 상태 안내를 지원합니다.
- 휴대폰 폭에서는 지역 버튼이 여러 줄로 자동 배치되어 화면 밖으로 나가지 않도록 구성했습니다.
